{
  "name": "mastery",
  "version": "1.0.0",
  "description": "Mastery — Duolingo-style daily practice for tech interviews (Supabase backend)",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "install:all": "npm install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
